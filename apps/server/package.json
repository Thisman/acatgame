{
  "name": "@acatgame/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -b",
    "test": "npm run build && node tests/run-tests.mjs"
  },
  "dependencies": {
    "@acatgame/game-core": "file:../../packages/game-core",
    "boardgame.io": "^0.50.2",
    "node-persist": "^3.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0"
  }
}
